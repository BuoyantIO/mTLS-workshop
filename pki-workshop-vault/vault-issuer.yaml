apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: vault-issuer
  namespace: cert-manager
spec:
  vault:
    path: pki/root/sign-intermediate
    server: http://192.168.0.1:8200
    auth:
      tokenSecretRef:
         name: my-secret-token
         key: token
